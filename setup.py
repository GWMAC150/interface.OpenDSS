from setuptools import setup

setup(name='OpenDSSAgent',
      version='0.1',
      description='OpenDSS agent for RIAPS',
      url='https://github.com/RIAPS/interface.OpenDSS',
      author='riaps',
      author_email='riaps@isis.vanderbilt.edu',
      license='MIT',
      packages=['dssa'],
      zip_safe=False)